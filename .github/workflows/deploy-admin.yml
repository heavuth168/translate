# Publishes the license admin website (web/) to GitHub Pages.
# One-time setup: GitHub repo → Settings → Pages → Source: "GitHub Actions".
name: Deploy license admin

on:
  push:
    branches: [main]
    paths: ["web/**", ".github/workflows/deploy-admin.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web
      - uses: actions/deploy-pages@v4
